spring:
  datasource:
    url: jdbc:h2:mem:studymarket;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  cache:
    type: simple

app:
  telegram:
    commands-enabled: false
    polling-enabled: false
